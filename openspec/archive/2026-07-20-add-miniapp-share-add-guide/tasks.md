## 1. 小程序引导组件与状态

- [x] 1.1 定位现有首页 / 全局入口结构，确认引导语接入点和是否需要共享组件。
- [x] 1.2 实现“添加到我的小程序”轻量引导语 UI，文案表达点击右上角入口添加到我的小程序。
- [x] 1.3 实现手工关闭交互，点击关闭后立即隐藏。
- [x] 1.4 实现关闭状态记忆，至少保证当前会话内不再重复展示。
- [x] 1.5 实现本地状态读写失败的安全降级，确保页面主功能不受影响。

## 2. 胶囊避让与页面适配

- [x] 2.1 复用微信菜单按钮、状态栏或项目统一 fallback 计算引导语位置。
- [x] 2.2 确认引导语容器、箭头和关闭入口不进入微信原生胶囊 reserve。
- [x] 2.3 确认首页品牌导航、搜索入口、Banner 和商品模块不被引导语遮挡。
- [x] 2.4 若引导接入非首页，确认返回按钮、页面标题、搜索入口和内容起点不被遮挡。
- [x] 2.5 检查 WXML / WXSS 中没有自绘模拟微信分享按钮、更多按钮、关闭按钮或系统胶囊。

## 3. 范围与数据边界

- [x] 3.1 确认本 Change 默认不新增 API、数据库表、字段、迁移、Web 管理端配置和 Orval 生成物。
- [x] 3.2 若实现阶段发现必须接入服务端配置或埋点，暂停实现并补充 API / DB / Orval / docs / tests 同步方案。
- [x] 3.3 如记录引导展示或关闭事件，确认事件不包含手机号、地址、微信身份标识、Authorization header、Cookie、`.env` 或真实客户数据。

## 4. 测试与设备 evidence

- [x] 4.1 补充小程序静态测试或等价检查，覆盖无伪系统胶囊、无伪分享按钮和关闭状态基础逻辑。
- [x] 4.2 记录 DevTools 320 pt 视口 evidence，确认引导语、关闭按钮、胶囊 reserve 和首屏内容无重叠。
- [x] 4.3 记录 DevTools 375 pt 视口 evidence，确认引导语、关闭按钮、胶囊 reserve 和首屏内容无重叠。
- [x] 4.4 记录 DevTools 430 pt 视口 evidence，确认引导语、关闭按钮、胶囊 reserve 和首屏内容无重叠。
- [x] 4.5 记录真机 evidence；若真机不可用，标记 `blocked` 或 `follow_up` 并说明原因，不得写作真机通过。
- [x] 4.6 运行与小程序静态检查相关的测试，并在验收报告中标明 API / DB / Orval 是否 N/A。

## 5. 文档与同步

- [x] 5.1 更新 Change trace，记录实现摘要、测试摘要、DevTools / 真机 evidence 状态和剩余风险。
- [x] 5.2 更新 REQ trace 与验收结论，保持 `openspec_changes` 状态同步。
- [x] 5.3 运行 Workflow Sync，确认 REQ、Change 与 Sprint 派生状态一致。
