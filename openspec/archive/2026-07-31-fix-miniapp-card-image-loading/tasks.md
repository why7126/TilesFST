## 1. 基线与根因确认

- [x] 1.1 采集小程序体验版首页、商品列表、搜索结果和品牌详情商品 Tab 的 Network evidence，记录 `/api/v1/miniapp/home`、`/api/v1/miniapp/products` 与 `/media/{object_key}` 的耗时、状态码和失败率。
- [x] 1.2 审计公开 SKU 主图与对象存储对象引用，输出缺图、对象不存在、缩略图缺失和原图体积过大的清单。
- [x] 1.3 确认当前小程序卡片组件图片属性、首屏渲染数量、分页加载策略和 `.ts`/`.js` 运行入口一致性。

## 2. 后端与媒体读取

- [x] 2.1 为公开 SKU 列表卡片提供缩略图优先的图片 URL 或等价优化 URL，保持详情页高清图能力不退化。
- [x] 2.2 如新增或持久化缩略图字段，同步 SQLite/MySQL schema、Pydantic Schema、数据库文档和迁移/回填脚本。
- [x] 2.3 为商品主图上传或历史对象补充缩略图生成、回填或按需生成策略，使用既有 MinIO 单 Bucket 与 `thumbnails/` 前缀。
- [x] 2.4 为 `/media/{object_key}` 图片响应补充合理缓存头、条件请求或对象版本策略，并保留视频 Range 与非视频读取兼容。
- [x] 2.5 增加媒体读取慢请求、失败请求和对象不存在的结构化观测，确保日志脱敏且不记录敏感 header。

## 3. 小程序体验修复

- [x] 3.1 为商品卡片图片启用小程序原生 `lazy-load` 或等价视口延迟加载策略。
- [x] 3.2 调整首页 `new_products`、`hot_products`、`all_products` 的图片加载优先级，避免非首屏卡片一次性触发全部图片请求。
- [x] 3.3 保留商品卡片统一占位图、失败降级、稳定比例和重试/兜底展示，不因缺图或慢网导致布局跳动。
- [x] 3.4 覆盖首页、商品列表、搜索结果、品牌详情商品 Tab 等复用商品卡片的入口一致性。

## 4. API、文档与测试

- [x] 4.1 若公开 SKU 列表 API 契约变更，同步 OpenAPI、Orval 或小程序 API 类型、接口文档和测试夹具。
- [x] 4.2 补充后端测试，覆盖缩略图 URL 选择、对象不存在回退、缓存头、媒体读取失败和公开数据过滤。
- [x] 4.3 补充小程序静态/页面测试，覆盖图片懒加载、占位、失败降级、首页多分区和复用卡片入口。
- [x] 4.4 运行相关 pytest、前端/小程序静态校验、OpenSpec 校验和目录结构校验。

## 5. 验收与追溯

- [x] 5.1 记录修复后体验版或等价环境的首屏图片完成时间、媒体失败率、对象缺失清单处理结果和对比截图/录屏。
- [x] 5.2 在 BUG trace、Change trace 和 Sprint 验收材料中记录实现、测试、环境、剩余风险和回滚策略。
- [x] 5.3 若确认本问题造成体验版发布事故或有可复用经验，补充 `docs/knowledge-base/incidents/` 或相关 best-practice 文档。
