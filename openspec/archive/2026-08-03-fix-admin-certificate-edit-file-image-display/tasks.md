## 1. 实现

- [x] 1.1 复核品牌证书编辑弹窗初始化逻辑，识别文件/图片展示模型缺口。
- [x] 1.2 规范既有证书文件状态，确保 PDF/兼容文件兜底不会显示 `证书文件已就绪`。
- [x] 1.3 将既有证书图片列表规范为图片卡片模型，包含预览 URL、缩略图 URL、稳定 ID、排序和 `is_main`。
- [x] 1.4 确保既有图片的主图标识、预览、删除和设为主图控件正确渲染。
- [x] 1.5 保留上传失败、文件缺失、不兼容文件和图片加载失败状态。
- [x] 1.6 确认编辑弹窗不展示 object key、原始文件名或无意义文件名噪音。
- [x] 1.7 如 API 响应缺少所需图片字段，在同一 Change 内同步更新 API schema、OpenAPI、Orval client、文档和测试。

## 2. 测试

- [x] 2.1 新增或更新 Web 测试，覆盖打开带 PDF/兼容文件和图片的既有证书编辑弹窗。
- [x] 2.2 新增或更新 Web 测试，断言既有文件兜底不会渲染 `证书文件已就绪`。
- [x] 2.3 新增或更新 Web 测试，覆盖图片列表、主图标识、预览/删除/设为主图控件和保存后重新打开行为。
- [x] 2.4 补充 `BUG-0089` 文件名/object-key 噪音的回归覆盖。
- [x] 2.5 运行品牌证书管理的定向前端测试。

## 3. 文档与追溯

- [x] 3.1 仅当 API、数据模型、上传行为或用户可见的已记录行为发生变化时，更新长期文档。
- [x] 3.2 实现完成后在 `BUG-0108` 记录验收 evidence。
- [x] 3.3 仅当实现揭示可复用事故模式时，评估是否更新 `docs/knowledge-base/incidents/`。
