# 任务清单

## 1. 后端候选值接口

- [x] 1.1 定义性能观测候选值 Pydantic Schema，覆盖固定枚举和动态候选项。
- [x] 1.2 在 Repository 中按时间范围查询 `app_version`、`page_key`、`device_class`、`network_type` 候选值。
- [x] 1.3 在 Service 中合并端类型、指标固定枚举和动态候选值，落实排序与数量限制。
- [x] 1.4 新增管理端路由 `GET /api/v1/admin/performance-events/filter-options`，复用系统管理员权限。

## 2. Web 管理端接入

- [x] 2.1 同步 OpenAPI / Orval，并更新性能 API 封装。
- [x] 2.2 性能观测页加载候选值，筛选区顺序固定为时间范围 > 端类型 > 版本号 > 页面 > 网络 > 指标。
- [x] 2.3 聚合查询携带新增筛选参数，并在筛选变化或重置后回到第一页。
- [x] 2.4 聚合列表字段顺序调整为页面 > 版本号 > 端类型 > 设备 > 网络 > 指标 > 样本 > P50 > P75 > P95 > P99 > 状态 > 操作。
- [x] 2.5 样本页上下文与样本列表字段顺序按 UI Contract 调整，继续保护敏感字段。

## 3. 文档与测试

- [x] 3.1 后端测试覆盖权限、时间范围、空数据、固定枚举、动态候选值和排序。
- [x] 3.2 Web 测试覆盖候选值加载、筛选顺序、字段顺序、分页重置、候选值失败和样本页安全字段。
- [x] 3.3 更新 `docs/03-api-index.md` 中管理端性能观测接口索引。
- [x] 3.4 如新增 DB 索引或字段，同步 SQLite/MySQL schema、`docs/04-database-design.md` 和测试；若无 DB 变更，在验收记录说明不涉及。

## 4. 验证与证据

- [x] 4.1 运行后端性能事件相关 pytest。
- [x] 4.2 运行 Web 性能观测页和样本页 Vitest。
- [x] 4.3 运行 OpenSpec 校验和语言校验。
- [x] 4.4 补充 1440x1024 性能观测页截图或等价视觉证据，验证筛选顺序、字段顺序和分页 DOM。
- [x] 4.5 补充候选值空态或加载失败状态证据。

## 验收返修记录

- [x] 2026-08-12 21:33:00 `/opsx-modify`：按验收反馈删除管理端性能观测页“设备”筛选项；后端候选值接口仍返回 `device_classes`，聚合列表和样本页仍展示设备字段。
