## 1. 基线与根因确认

- [x] 1.1 定位品牌详情页商品 Tab、商品列表页分类入口、搜索商品结果页当前调用的接口、查询参数和排序逻辑。
- [x] 1.2 准备包含不同 `published_at`、相同 `published_at`、空 `published_at`、不同 SKU ID 的公开 SKU 样本。
- [x] 1.3 记录修复前品牌详情页、搜索商品结果页、一级分类商品列表、二级分类商品列表的排序差异样本。

## 2. 后端与接口排序修复

- [x] 2.1 将小程序公开 SKU 列表默认排序统一为 `published_at` 升序、SKU ID 升序，`published_at` 为空时使用 `created_at` 兜底。
- [x] 2.2 覆盖品牌过滤、一级分类聚合、二级分类精确和普通搜索关键词命中场景，避免各入口维护不同默认排序。
- [x] 2.3 保持新品榜、热销榜、价格排序和明确的搜索相关性排序不被默认排序误覆盖。
- [x] 2.4 确保分页查询在默认排序下稳定，不因相同时间字段导致跨页重复、漏项或顺序漂移。
- [x] 2.5 若 API 参数、响应字段或排序语义文档发生变化，同步 OpenAPI、Orval、docs/03-api-index.md 和相关测试夹具。

## 3. 小程序入口与页面修复

- [x] 3.1 确认搜索商品结果页、分类商品列表页进入商品列表页时携带的 `keyword`、`categoryId`、`categoryLevel`、`sourcePage` 等上下文正确。
- [x] 3.2 移除或调整小程序端与后端默认排序冲突的本地排序逻辑。
- [x] 3.3 确认分页加载更多追加后保持整体顺序稳定。
- [x] 3.4 确认首页“全部产品”列表保持既有排序策略，不被本 BUG 修复改变。

## 4. 测试与验收

- [x] 4.1 补充后端公开 SKU 查询排序测试，覆盖品牌、搜索、一级分类、二级分类和分页。
- [x] 4.2 补充小程序页面、静态测试或等价验收脚本，覆盖搜索商品结果页、分类商品列表页、品牌详情页商品 Tab。
- [x] 4.3 补充回归测试或验收记录，确认首页“全部产品”、新品榜、热销榜、价格排序或相关性排序不受影响。
- [x] 4.4 运行相关 pytest、小程序静态校验、OpenSpec 校验和目录结构校验。

## 5. 追溯与知识沉淀

- [x] 5.1 在 BUG trace、Change trace 和 Sprint 验收材料中记录实现、测试、验收环境和剩余风险。
- [x] 5.2 若修复过程中发现商品列表排序契约有跨入口复用价值，补充 `docs/knowledge-base/` 中的列表排序最佳实践或复盘记录。
