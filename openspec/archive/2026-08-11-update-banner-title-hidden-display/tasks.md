## 1. 管理端 Banner 标题隐藏

- [x] 1.1 调整 `BannerFormModal` 状态与初始化逻辑，隐藏 Banner 标题输入框，编辑时保留历史 title 作为内部字段。
- [x] 1.2 实现内部标题生成或保留策略，确保创建/更新 payload 不因标题缺失失败，冲突时有兜底。
- [x] 1.3 移除标题必填和标题重复的运营可见校验提示，保存失败文案不得要求运营填写隐藏字段。
- [x] 1.4 调整 Banner 列表第一列与搜索 placeholder，使用缩略图、展示位置、跳转类型、跳转目标、排序或更新时间提供识别上下文。

## 2. 小程序前台标题遮罩移除

- [x] 2.1 调整小程序首页 Banner 轮播，有有效 Banner 图片时不渲染 `item.title` 主标题，并清理无意义空容器。
- [x] 2.2 调整小程序品牌列表页 Banner 轮播，有有效 Banner 图片时不渲染 `item.title` 主标题，并保持指示点和跳转可用。
- [x] 2.3 保留无 Banner 数据时首页和品牌列表页的原有兜底 Hero 文案。

## 3. API / Orval 判断

- [x] 3.1 优先保持现有 API schema 不变；若前端自动生成内部 title 即可满足保存，记录“不需要 Orval”的依据。
- [x] 3.2 未采用后端缺省标题方案，Pydantic Schema、OpenAPI、Orval、接口文档和后端集成测试无需变更。

## 4. 测试与验收

- [x] 4.1 补充管理端 Vitest：弹窗无标题字段、保存不因标题为空阻断、payload 内部标题策略、列表识别和搜索文案。
- [x] 4.2 补充小程序静态测试或结构断言：首页和品牌列表页 Banner 有图时不渲染标题文本。
- [x] 4.3 回归 Banner 图片上传状态机、即时回显、fixed toast、DS confirm、弹窗宽度和矮视口滚动。
- [x] 4.4 已在归档前补充用户确认的真机 UI evidence：小程序首页与品牌列表页 Banner 去标题遮罩展示完成真机验收。

## 5. 文档与校验

- [x] 5.1 更新相关使用说明或接口文档中“Banner 标题”语义，明确其为内部兼容字段。
- [x] 5.2 运行 `python scripts/validate-openspec-language.py`。
- [x] 5.3 运行相关前端、小程序和后端测试；如 API 不变，说明未运行 Orval 的原因。
