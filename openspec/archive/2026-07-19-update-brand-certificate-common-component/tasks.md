## 1. Component Contract

- [x] 1.1 梳理现有品牌证书页面中的文件类型判断、状态 label、日期格式化和预览 helper，确定可复用边界。
- [x] 1.2 设计品牌证书展示模型 props，优先使用 `BrandCertificateItem` 的窄类型或 `Pick<>`，避免暴露分页、筛选、权限和弹窗内部状态。
- [x] 1.3 在管理端品牌证书业务组件目录建立清晰导出路径，覆盖缩略图、摘要、有效期、有效状态、展示状态、预览入口和文件卡片。

## 2. Component Implementation

- [x] 2.1 实现证书缩略图，覆盖图片、PDF、文件 fallback、图片加载失败和稳定尺寸。
- [x] 2.2 实现证书摘要，覆盖证书名称、证书编号、文件名 fallback、可选品牌名称和长文本截断。
- [x] 2.3 实现有效期文本、有效状态 Badge 和展示状态 Badge，复用服务端状态与现有管理端语义。
- [x] 2.4 实现预览入口回调，覆盖 URL 存在、URL 缺失和失败原因返回，不生成未授权对象存储直连地址。
- [x] 2.5 实现文件卡片 `idle`、`uploading`、`done`、`failed` 状态，只展示状态并触发回调。

## 3. Page Integration

- [x] 3.1 将 `/admin/brand-certificates` 列表“证书”列替换为通用缩略图与摘要。
- [x] 3.2 将列表“有效期”“有效状态”“前台展示”列替换为通用展示方法或组件。
- [x] 3.3 将新增/编辑证书弹窗文件展示区使用或对齐通用文件卡片。
- [x] 3.4 保持筛选、分页、权限、保存、删除、显示/隐藏确认、固定 toast 和上传 API 调用仍由页面容器负责。

## 4. Verification

- [x] 4.1 补充组件单元测试，覆盖图片、PDF、fallback、未知状态、URL 缺失和文件卡片四状态。
- [x] 4.2 补充或更新品牌证书页回归测试，覆盖列表列、弹窗文件卡片、预览入口和危险操作确认。
- [x] 4.3 在 1440x1024 视口验收分页 DOM、指标卡 DOM、弹窗宽度、fixed toast 和无 `window.confirm` 回归。
- [x] 4.4 通过 Docker Web 入口 `http://localhost:3000` 验收合法小文件上传即时回显和超限文件业务错误。
- [x] 4.5 检查本 change 未修改 API、数据库、MinIO 策略、Orval 生成物和小程序代码。
