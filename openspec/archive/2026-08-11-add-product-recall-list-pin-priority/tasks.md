---
change_id: add-product-recall-list-pin-priority
source_requirement: REQ-0103-product-recall-list-pin-priority
related_sprint: sprint-022
status: applied
created_at: 2026-08-07 23:12:00
updated_at: 2026-08-08 07:17:00
---

# 实施任务

## 1. 数据库与模型

- [x] 1.1 为 `tiles` 增加 `recall_pin_sort_order`、`recall_pin_starts_at`、`recall_pin_ends_at` 字段，默认值和可空语义与设计保持一致。
- [x] 1.2 同步 SQLite schema、SQLite migration、MySQL baseline / migration 或 drift 修复路径。
- [x] 1.3 更新数据库文档，记录字段语义、默认值、有效期空值语义和索引策略。
- [x] 1.4 补充字段正整数、默认 `9999`、时间范围校验的模型 / repository / service 测试。

## 2. 管理端 SKU 配置

- [x] 2.1 更新管理端 SKU create/update/detail 的 Pydantic Schema 和 API 实现，支持召回置顶配置保存与回显。
- [x] 2.2 在 SKU 新建 / 编辑弹窗新增运营配置字段组，复用现有 Design System 表单控件。
- [x] 2.3 实现字段级校验：排序值只允许正整数，开始时间不得晚于结束时间。
- [x] 2.4 验证保存成功 / 失败反馈不引起页面或弹窗布局位移，矮视口弹窗滚动无回归。

## 3. 小程序公开列表排序

- [x] 3.1 在 `/api/v1/miniapp/products` 普通商品列表查询中加入召回置顶排序层，并确保在分页前生效。
- [x] 3.2 在 `/api/v1/miniapp/search` 的 SKU 结果中加入同一召回置顶排序原则。
- [x] 3.3 确保召回置顶计算发生在公开条件过滤和请求筛选之后。
- [x] 3.4 确保每次请求最多 4 个生效置顶 SKU；超出上限按排序值和稳定兜底裁定。
- [x] 3.5 确保新品榜、热销榜、价格升序 / 降序、收藏列表、搜索实时联想、管理端 SKU 列表和店主 Web 列表不应用召回置顶。
- [x] 3.6 确认小程序端不新增置顶标识、不做跨页本地重排。

## 4. API、OpenAPI 与 Orval

- [x] 4.1 同步 OpenAPI schema，覆盖管理端 SKU 请求 / 响应字段变化。
- [x] 4.2 运行 Orval 生成管理端 Web API 类型和客户端代码。
- [x] 4.3 更新 API 文档中管理端 SKU 字段和小程序列表排序说明。
- [x] 4.4 复核小程序公开响应不暴露召回状态或内部排序解释字段。

## 5. 测试与验收

- [x] 5.1 后端测试覆盖普通商品列表置顶、搜索 SKU 结果置顶、排序值相同、默认值、有效期未开始、有效期已结束、超过上限。
- [x] 5.2 后端测试覆盖品牌、类目、关键词、规格、价格和公开状态过滤下召回商品不越界展示。
- [x] 5.3 回归测试覆盖新品榜、热销榜和价格排序不被召回置顶覆盖。
- [x] 5.4 小程序测试或验收证据覆盖按接口顺序展示、加载更多无重复 / 漏项 / 跳动、无新增 UI 标识。
- [x] 5.5 管理端测试覆盖字段渲染、保存回显、校验错误、toast 反馈和弹窗矮视口滚动。
- [x] 5.6 运行 OpenSpec、语言、目录结构、后端测试、Web 测试和必要的小程序静态检查。

## 6. 追踪与发布准备

- [x] 6.1 更新 Change `trace.md` 的实现证据、测试证据和验收结果。
- [x] 6.2 若数据库字段进入发布，发布准备阶段补充 MySQL schema drift 或等价数据库校验证据。

## 验收返修记录

- [x] 2026-08-08 返修：SKU 编辑弹窗中召回排序字段移至参考价格后，标签调整为“排序”，标记必填，并增加问号帮助图标 hover 说明。
- [x] 2026-08-08 返修：SKU 列表在状态字段前新增“排序”列，展示 `recall_pin_sort_order`。
- [x] 2026-08-08 返修：SKU 列表状态标签增加单行显示约束，避免“已上架”“已下架”换行。
- [x] 2026-08-08 返修：排序值非法时在排序字段下方显示红色字段级提示“排序值必须为正整数”，不再展示到弹窗顶部全局错误区。
