## 1. 平台能力确认

- [x] 1.1 确认微信小程序 `video` 组件全屏入口、VideoContext 全屏控制、全屏态长按事件和自定义菜单能力，并在实现记录中写明结论。
- [x] 1.2 确认当前详情接口视频 URL 是否可被 `wx.downloadFile` 下载到临时文件并用于保存相册，记录权限、域名、格式和文件大小限制。
- [x] 1.3 决定使用原生全屏入口、自定义全屏入口，或两者结合；若使用自定义入口，明确位置、触控面积和与原生控制条关系。

## 2. 小程序实现

- [x] 2.1 更新 `src/miniapp/pages/tile-detail/index.wxml` 的视频媒体结构，确保视频全屏入口明确可感知且不遮挡媒体计数、原生胶囊、返回按钮或底部操作栏。
- [x] 2.2 更新 `src/miniapp/pages/tile-detail/index.ts`，实现全屏入口点击、VideoContext 全屏控制、退出全屏后上下文保持和页面隐藏 / 切换媒体暂停策略。
- [x] 2.3 根据平台能力实现全屏态长按菜单或等价入口，覆盖“转发给朋友”“保存视频”“取消”三个用户意图。
- [x] 2.4 复用 SKU 详情页既有分享路径和分享对象，确保视频全屏态触发“转发给朋友”时保留 `skuId` 与 `source=share`。
- [x] 2.5 实现保存视频流程或降级提示，覆盖下载中、防重复触发、保存成功、权限拒绝、网络异常、视频暂不可保存和平台不支持。
- [x] 2.6 同步更新运行时 `src/miniapp/pages/tile-detail/index.js`，确保 `.ts` 源与微信开发者工具实际加载入口一致。

## 3. 埋点与安全

- [x] 3.1 补充或预留视频全屏入口点击、菜单动作、保存成功 / 失败埋点，确保埋点失败不阻断全屏、分享、保存或取消。
- [x] 3.2 验证视频 URL 继续来自详情接口安全可播放 URL，不拼接 raw object key，不暴露 bucket、endpoint、密钥或未授权对象存储路径。
- [x] 3.3 若实现需要新增签名下载 URL、媒体字段或 API contract，同步后端、OpenAPI、Orval、docs 和 tests；若不需要，在验收记录写明 N/A。

## 4. 测试与验收

- [x] 4.1 补充小程序静态测试，覆盖视频全屏入口属性、分享路径、保存视频降级提示或等价关键逻辑。
- [x] 4.2 补充 `.ts` / `.js` 运行入口同步检查，避免商品详情页视频全屏、菜单、分享和保存逻辑漂移。
- [x] 4.3 记录 DevTools 320 / 375 / 430 pt evidence，确认视频全屏入口不遮挡媒体计数、原生胶囊、返回按钮或底部操作栏。
- [x] 4.4 记录至少一台真机 evidence，覆盖全屏入口、全屏播放、长按菜单或降级入口、转发、保存成功或失败提示、退出全屏回到详情页。
- [x] 4.5 若真机暂不可用，验收状态标记 `real_device_follow_up` 或 `blocked`，不得把静态测试或 DevTools 截图写成真机通过。

## 5. 收尾校验

- [x] 5.1 运行相关小程序静态测试和必要的后端 / API 测试，确认无 API/DB/Orval 变化时写明 N/A。
- [x] 5.2 更新 Change acceptance / implementation 记录，说明平台能力确认结果、降级策略、evidence 路径和剩余风险。
- [x] 5.3 运行 OpenSpec 校验和 Workflow Sync，确认 REQ、Change 与 Sprint 状态一致。
