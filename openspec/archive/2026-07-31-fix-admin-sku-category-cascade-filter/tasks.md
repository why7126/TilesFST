## 1. Implementation

- [x] 1.1 调整管理端 SKU 页「类目」筛选为级联选择控件，数据源使用完整类目树。
- [x] 1.2 支持选择任意层级类目，并展示当前选择路径。
- [x] 1.3 保留「全部类目」/清空选择和重置筛选能力。
- [x] 1.4 调整后端 SKU 列表类目过滤逻辑，使父类目筛选包含自身及所有子孙类目的 SKU。
- [x] 1.5 确保叶子类目筛选、关键词、品牌、状态、素材完整度、分页和默认排序可组合。
- [x] 1.6 若 API 参数语义或参数名变化，同步 Pydantic Schema、OpenAPI、Orval、docs/03-api-index.md 和前端调用。

## 2. Tests

- [x] 2.1 补充或更新 `TileSkuManagementPage` 前端测试，覆盖级联控件展示各层级类目。
- [x] 2.2 补充或更新前端测试，覆盖选择父类目、子类目和清空类目后的请求参数。
- [x] 2.3 补充或更新后端 pytest，覆盖父类目筛选包含所有子孙类目 SKU。
- [x] 2.4 补充或更新后端 pytest，覆盖子类目筛选与关键词、品牌、状态、素材完整度组合过滤。
- [x] 2.5 如发生 API 契约变化，补充 OpenAPI/Orval 生成校验或相关前端类型使用测试。

## 3. Documentation

- [x] 3.1 如 API 参数语义变化，同步 `docs/03-api-index.md`。
- [x] 3.2 如实现选择应用层子树解析或递归查询，按需在 Change design 或实现说明中记录 SQLite/MySQL 兼容策略。

## 4. Validation

- [x] 4.1 运行相关后端 pytest。
- [x] 4.2 运行相关前端 Vitest/Testing Library 测试。
- [x] 4.3 视检管理端 SKU 页筛选区，确认级联控件、重置按钮、表格列和操作区无布局遮挡。
- [x] 4.4 判断是否需要沉淀到 `docs/knowledge-base/incidents/`；若无跨模块复用价值，在执行输出中说明不适用。

## 验收返修记录

- [x] 2026-07-31 20:53:08 `/opsx-modify`：按验收反馈将类目筛选从多个并排层级下拉改为单个级联下拉触发框；点击有下级的当前类目时，在同一下拉层右侧展开下级类目面板。
- [x] 2026-07-31 20:53:08 `/opsx-modify`：更新前端测试，覆盖单下拉框、父级点击展开右侧子级面板、父/子类目筛选参数和清空能力。
- [x] 2026-07-31 21:15:20 `/opsx-modify`：按验收反馈移除筛选项下方「当前：xxx」文案，并提升 SKU 筛选卡与类目下拉层层级，避免下拉层被 SKU 列表遮挡。
- [x] 2026-07-31 21:25:00 `/opsx-modify`：按验收反馈统一品牌、类目、状态筛选下拉的触发框、菜单位置、选项样式和选中态，并补充前端一致性测试。
