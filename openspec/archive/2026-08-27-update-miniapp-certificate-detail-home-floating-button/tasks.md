## 1. 页面接入

- [x] 1.1 在 `src/miniapp/pages/certificate-detail/index.json` 声明 `home-floating-button` 组件。
- [x] 1.2 在 `src/miniapp/pages/certificate-detail/index.wxml` 挂载 `home-floating-button`，并传入 `offset="list"`。
- [x] 1.3 确认证书详情页原有 `custom-navigation`、证书媒体区、证书信息、品牌入口、错误态和分享能力未被新增组件改写。

## 2. 状态与交互验证

- [x] 2.1 验证证书详情正常态、加载态、网络失败、证书不可查看、证书不存在、图片失败和分享直达状态均保留可恢复的回首页路径。
- [x] 2.2 验证点击悬浮按钮沿用 `home-floating-button` 既有 `wx.switchTab` 与兜底策略，不新增页面私有跳转逻辑。
- [x] 2.3 验证快速重复点击、返回首页后再次进入证书详情页、导航失败兜底后的导航锁恢复。

## 3. 静态检查与同步

- [x] 3.1 补充或更新小程序静态检查，覆盖证书详情页 `index.json` 组件声明、`index.wxml` 组件引用和页面路径注册。
- [x] 3.2 静态检查或等价脚本确认 `home-floating-button` 在证书详情页使用 `offset="list"`。
- [x] 3.3 确认 `index.ts` 与 `index.js` 无行为漂移；如实现需调整逻辑，两个文件必须同步。
- [x] 3.4 回归已接入返回首页悬浮按钮的商品详情页、品牌详情页和商品列表页，确认全局组件行为未被改变。

## 4. UI 与设备证据

- [x] 4.1 记录 DevTools 320 / 375 / 430 pt evidence，覆盖标题、原生胶囊 reserve、内容 offset、品牌入口同宽、证书信息非避让排版和悬浮按钮位置一致；证书信息字段被按钮局部覆盖为验收允许项。
- [x] 4.2 记录分享直达页面栈兜底、左上返回兜底和悬浮返回首页入口的操作摘要。
- [x] 4.3 真机 evidence 不可用时，在 Change trace 或验收记录中标记 `blocked` 或 `follow_up`，不得写作真机通过。

## 5. 文档与收口

- [x] 5.1 更新 Change `trace.md` 的实现证据、静态检查结果、DevTools evidence 和剩余风险。
- [x] 5.2 运行 OpenSpec 与语言校验，确保 proposal、design、tasks 和 delta specs 均通过。
- [x] 5.3 完成 Workflow Sync，确认 REQ-0125、Change 和 `sprint-026` scope 已互相回填。

## 验收返修记录

- [x] 2026-08-25 23:24:38 品牌卡片宽度返修：移除证书详情页 `.brand-card-section` 右侧缩窄规则，保持品牌卡片外框与上方标题卡、下方证书信息卡同宽；保留页面底部滚动安全留白和证书信息卡右侧字段安全区，继续避免【返回首页】悬浮按钮遮挡内容。
- [x] 2026-08-26 08:13:44 复验截图确认品牌卡片外框已同宽，但 320 / 375 pt 中【返回首页】悬浮按钮仍压到品牌卡右侧箭头区域；改为在 `brand-card` 内新增可选 `home-floating-safe` 内容安全区，并仅由证书详情页传入，不再缩窄品牌卡外框。
- [x] 2026-08-26 08:21:44 根据验收反馈「返回上一步的效果，不要调整右侧箭头」，撤回 `brand-card` 的 `customClass/home-floating-safe` 内部箭头避让方案，恢复上一版：品牌卡片外框同宽，证书详情页仅保留页面底部安全留白和证书信息卡右侧字段安全区。
- [x] 2026-08-26 08:28:56 根据验收反馈「证书信息卡不需要为返回首页悬浮按钮做右侧避让，按钮遮挡证书信息可接受」，将 `.panel` padding 恢复为 `26rpx`；保留品牌卡片外框同宽，不调整右侧箭头。
- [x] 2026-08-26 08:32:09 复验截图确认 DevTools 320 / 375 / 430 pt 下品牌卡片外框同宽、右侧箭头不额外调整、证书信息卡恢复非避让排版；按钮局部覆盖证书信息字段为验收允许项。
