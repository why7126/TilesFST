## 1. Implementation

- [x] 1.1 确认管理端类目树响应中的直接子类目数量字段；若 OpenAPI/Orval 未暴露，补齐 `children_count` 或当前稳定字段名。
- [x] 1.2 调整管理端类目树组件计数绑定，节点右侧数字使用直接子类目数量，不使用 `sku_count`。
- [x] 1.3 调整“全部类目”入口右侧数字，显示顶层类目数量。
- [x] 1.4 保留 `sku_count` 作为商品/SKU 数量字段，不改变删除规则、列表统计、筛选联动和权限边界。
- [x] 1.5 如需要展示商品数量，将其移动到独立位置或明确标签，避免与类目数量混用。

## 2. Tests

- [x] 2.1 补充或更新管理端类目树组件测试：一级类目右侧数字等于直接子类目数量。
- [x] 2.2 补充或更新管理端类目树组件测试：叶子类目显示 `0`，即使存在商品数量也不显示商品数。
- [x] 2.3 补充或更新管理端页面测试：“全部类目”入口显示顶层类目数量。
- [x] 2.4 若 API 契约补齐，补充后端接口测试并运行 OpenAPI/Orval 生成校验。

## 3. Documentation

- [x] 3.1 若 API 响应字段或 OpenAPI 契约变化，更新 `docs/03-api-index.md`。
- [x] 3.2 若数据库结构不变，明确无需更新 `docs/04-database-design.md`。
- [x] 3.3 归档前同步 `openspec/specs/tile-category-management/spec.md` 与 `openspec/specs/web-client/spec.md`。

## 4. Validation

- [x] 4.1 运行相关后端测试或说明不适用。
- [x] 4.2 运行相关前端测试。
- [x] 4.3 视检管理端类目树，确认一级类目、叶子类目、“全部类目”计数口径正确，展开/折叠和筛选联动不回归。
- [x] 4.4 判断是否需要沉淀到 `docs/knowledge-base/incidents/`；若无跨模块复用价值，在执行输出中说明不适用。

## 验收返修记录

- [x] 2026-07-31 17:18:45 `/opsx-modify`：根据验收反馈，将“全部类目”入口放入与一级类目一致的 `tree-node-row level-1` 布局并补左侧 `tree-toggle-spacer`，确保右侧数值与一级类目数值位置对齐；补充组件测试与 web-client delta spec。无需更新 API、DB、Docker、MinIO 或长期接口文档。
- [x] 2026-07-31 17:24:07 `/opsx-modify`：根据验收反馈，保留“全部类目”右侧数值列位置不变，仅通过 `all-categories-node` 局部样式将“全部类目”文字左移，与类目树标题文字左对齐；补充组件样式契约测试。无需更新 API、DB、Docker、MinIO 或长期接口文档。
- [x] 2026-07-31 17:29:59 `/opsx-modify`：根据验收反馈，移除“全部类目”文字负向偏移和左侧占位，改为整行按钮加左内边距，确保选中态边框完整包住文字和数值，同时保持右侧数值位置不变；更新组件样式契约测试。无需更新 API、DB、Docker、MinIO 或长期接口文档。
