## 1. OpenSpec 与追溯

- [x] 1.1 确认 `BUG-0066`、`REQ-0046`、`sprint-009` 与本 Change 双向追溯完整。
- [x] 1.2 复核本 Change 不新增 Web/Admin 搜索配置中心、DB 结构或对象存储能力。
- [x] 1.3 运行 OpenSpec 校验并修复规格格式问题。

## 2. 小程序搜索页原型对齐

- [x] 2.1 对齐搜索页顶部结构：返回按钮、搜索框、取消/搜索按钮、安全区和触控尺寸。
- [x] 2.2 复用 `components/search-entry`，或保持独立搜索页与该组件在关键词、清空、提交、取消/返回、禁用态、`scope` 和 `sourcePage` 上等价。
- [x] 2.3 按当前验收口径修复搜索首页，展示最近搜索、热门搜索，不展示最近浏览，并支持最近搜索单条删除和全部清空。
- [x] 2.4 按当前验收口径修复联想态，仅覆盖品牌和 SKU 分组，不展示最近搜索、普通关键词、类目、规格或证书。
- [x] 2.5 按当前验收口径修复结果态，展示综合、品牌、SKU、证书 Tab，综合默认激活且横向可滚动，不展示类目 Tab。
- [x] 2.6 渲染综合结果最佳匹配和 `sections` 分区，覆盖品牌、SKU、证书分区，并隐藏 0 条分区。
- [x] 2.7 修复 SKU 卡片展示与点击：主图、产品名称、SKU 编码、品牌、规格、参考价格和整卡进入详情。
- [x] 2.8 移除搜索结果页快捷筛选、筛选按钮和筛选抽屉。
- [x] 2.9 按 `05-search-empty` 修复无结果态，展示当前关键词、搜索图标、调整建议列表和推荐搜索词。
- [x] 2.10 验证搜索首页、联想、加载、结果、筛选、失败和无结果状态切换无明显布局跳动、内容重叠或底部遮挡。

## 3. 测试与验收

- [x] 3.1 补充小程序静态或单元测试，覆盖搜索通用组件应用或等价行为。
- [x] 3.2 补充测试覆盖综合分区渲染、联想类型限制、最佳匹配优先级、结果页无筛选和无类目 Tab。
- [x] 3.3 补充测试覆盖无结果页结构、搜索范围外入口不展示、证书不参与联想。
- [x] 3.4 对照 `REQ-0046` 的 5 个 HTML/PNG 原型记录验收证据摘要。
- [x] 3.5 若修复中发现 API 契约缺口，先补充 API delta、docs、OpenAPI/Orval 和接口测试后再继续。

## 4. 文档与收尾

- [x] 4.1 更新 BUG-0066、REQ-0046、Sprint 和本 Change 的追溯状态。
- [x] 4.2 修复完成后评估是否需要写入 `docs/knowledge-base/incidents/`，沉淀“原型验收任务误判”的复发预防经验。
- [x] 4.3 汇总测试命令、原型对照结果和影响范围，保持成功路径 compact summary。
