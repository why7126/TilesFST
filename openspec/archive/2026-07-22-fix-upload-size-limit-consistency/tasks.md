## 1. 规格与配置

- [x] 1.1 确认图片、视频、文档三类上传的产品允许大小上限。
- [x] 1.2 新增或等价实现文档类上传大小 effective 配置，例如 `media.max_file_size_mb` / `MAX_FILE_SIZE_MB`。
- [x] 1.3 更新 `EffectiveSettingsService` 媒体分组，使图片、视频、文档限制均可读取并可按需保存 / 恢复默认。
- [x] 1.4 同步 `.env.example`、媒体文档、对象存储文档和部署文档中的上传限制说明。

## 2. 后端上传修复

- [x] 2.1 将品牌证书 / PDF 上传从硬编码 `CERTIFICATE_MAX_SIZE_MB = 20` 改为文档类 effective 限制。
- [x] 2.2 确认图片、视频、文档上传入口均经后端鉴权、MIME Type 校验、对象 Key 校验和对象存储适配层。
- [x] 2.3 统一超限、类型不支持、对象存储不可用的错误响应和可诊断提示。
- [x] 2.4 确认 `images/`、`videos/`、`files/` 前缀和单 Bucket 策略不回退。
- [x] 2.5 如新增 API 字段、错误码或上传限制查询能力，同步 OpenAPI、Orval、`docs/03-api-index.md` 和 API 测试。

## 3. 管理端与提示

- [x] 3.1 更新管理端上传入口提示，使图片、视频、文档限制与后端 effective 值一致。
- [x] 3.2 若系统设置 media Tab 增加文档类限制，补齐表单、校验、保存、恢复默认和只读说明。
- [x] 3.3 上传失败时展示清晰错误状态，允许用户调整文件后重试。

## 4. 测试与回归

- [x] 4.1 后端测试覆盖约 23M 图片在允许配置下上传成功，超过图片上限时返回 `FILE_SIZE_EXCEEDED`。
- [x] 4.2 后端测试覆盖约 23M 视频在视频入口上传成功，不支持 MIME 被拒绝。
- [x] 4.3 后端测试覆盖约 23M PDF / 文档在允许配置下上传成功，超过文档上限时返回清晰错误。
- [x] 4.4 后端测试覆盖对象存储写入失败时返回 `STORAGE_UNAVAILABLE` 或等价统一错误，不泄露内部路径。
- [x] 4.5 前端测试覆盖上传限制提示和上传失败状态。
- [x] 4.6 Docker / Nginx / 生产代理配置检查确认请求体大小和超时不低于后端最大 effective 上传限制。

## 5. 验收与追踪

- [x] 5.1 验证图片、视频、文档约 23M 文件符合产品规则时可成功上传，或被清晰拒绝。
- [x] 5.2 验证上传成功对象可通过 `/media/{object_key}` 受控读取。
- [x] 5.3 更新 `BUG-0073` trace、Change trace 与验收证据。
- [x] 5.4 修复完成后评估是否需要沉淀到 `docs/knowledge-base/incidents/`；若无复用价值，在验收输出中说明不新增知识库条目。
