## 1. 小程序全屏入口修复

- [x] 1.1 调整 SKU 详情页视频全屏入口，优先复用当前 `<video>` / `VideoContext` 上下文进入全屏播放态。
- [x] 1.2 保持内嵌视频由用户主动播放，不默认自动播放。
- [x] 1.3 保持视频播放期间轮播不自动切换，页面隐藏、锁屏、跳转或返回时暂停当前视频。
- [x] 1.4 为全屏切换等待、失败或平台限制提供明确反馈或降级提示。

## 2. 回归与安全边界

- [x] 2.1 确认图片轮播、图片预览、收藏分享和推荐跳转不回归。
- [x] 2.2 确认视频 URL 继续来自后端安全可播放链路，不暴露原始 object key、对象存储 endpoint、bucket 或未授权素材路径。
- [x] 2.3 若实现涉及 API 或响应字段变化，同步 OpenAPI / Orval / docs / tests；若不涉及，在实现输出中明确说明不需要。

## 3. 测试与验收证据

- [x] 3.1 更新小程序静态测试，覆盖全屏入口关键 API 或降级逻辑，以及 `index.ts` / `index.js` 同步。
- [x] 3.2 运行相关小程序静态测试，例如 `uv run pytest tests/test_miniapp_static.py`。
- [x] 3.3 使用真机或可接受的设备证据记录至少一个实际 SKU 的内嵌播放首帧耗时与全屏切换首帧耗时。
- [x] 3.4 在 BUG-0084 或 Change trace / acceptance 中记录机型、微信版本、网络类型、视频大小、格式、编码、时长和验收结论。

## 4. 知识沉淀

- [x] 4.1 修复后评估是否需要沉淀到 `docs/knowledge-base/incidents/`；若不需要，在验收记录中说明原因。
