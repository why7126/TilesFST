## 1. API 与数据契约

- [x] 1.1 确认可复用的品牌、Banner、商品列表和品牌证书数据源，记录是否需要新增公开接口。
- [x] 1.2 如需新增品牌入口/详情接口，实现小程序公开品牌列表和品牌详情 API，并过滤不可公开品牌和内部字段。
- [x] 1.3 如需扩展品牌轮播，复用现有 Banner 管理能力或新增品牌页展示位置，并同步枚举、校验和兼容策略。
- [x] 1.4 为品牌商品 Tab 接入按 `brandId` 查询公开 SKU 的分页接口或既有商品列表接口。
- [x] 1.5 为品牌证书 Tab 接入按 `brandId` 查询可公开证书的列表接口，返回受控证书图片或预览 URL。
- [x] 1.6 同步 OpenAPI、Orval、`docs/03-api-index.md` 和错误码/接口治理文档。

## 2. 小程序页面实现

- [x] 2.1 新增品牌入口页路由、页面配置、`.ts` 与 `.js` 运行入口。
- [x] 2.2 在品牌入口页接入与首页一致的 swiper 轮播、指示器、图片比例和降级策略。
- [x] 2.3 在品牌入口页实现一行 2 个品牌卡片列表，复用或对齐 `miniapp-brand-card-component`。
- [x] 2.4 新增品牌主页/详情页路由，支持 `brandId` 参数、异常参数和分享直达。
- [x] 2.5 在品牌主页上半部分实现品牌图片、品牌名称、品牌介绍和图片/字段兜底。
- [x] 2.6 在品牌主页下半部分实现“商品 / 证书”Tab，确保 Tab 状态相互独立。

## 3. 列表、跳转与埋点

- [x] 3.1 商品 Tab 接入双列商品卡片、分页、下拉刷新、上拉加载更多、空态和错误重试。
- [x] 3.2 商品卡片点击进入 SKU 详情页，并携带 `brandId`、`sourcePage`、`sourceModule`、`index` 和可用 `requestId`。
- [x] 3.3 证书 Tab 接入证书列表、图片占位、空态、错误态和证书预览或详情跳转。
- [x] 3.4 接入品牌入口页浏览、品牌轮播点击、品牌卡片点击、品牌主页浏览、Tab 切换、商品点击和证书点击埋点。
- [x] 3.5 确保埋点失败不阻断页面加载、Tab 切换、预览或详情跳转。

## 4. UI 与小程序设备验收

- [x] 4.1 按原型和 `rules/ui-design.md` 校验暗色旗舰风、品牌金强调、低圆角、稳定图片比例和长文本截断。
- [x] 4.2 按 `docs/knowledge-base/best-practices/miniapp-custom-navigation.md` 校验自定义导航、状态栏、胶囊 reserve、返回兜底和页面 offset。
- [x] 4.3 记录 DevTools 320 / 375 / 430 pt evidence，覆盖品牌入口页、品牌主页商品 Tab、品牌主页证书 Tab、加载态、空态和错误态。
- [x] 4.4 如真机不可用，在验收材料中标记 blocked 或 follow_up；不得将静态测试写作真机通过。

## 5. 测试与文档

- [x] 5.1 补充后端接口测试，覆盖公开字段过滤、不可公开品牌/SKU/证书过滤、分页和异常参数。
- [x] 5.2 补充小程序静态测试，覆盖新增页面 `.ts` / `.js` 运行入口不为空模板。
- [x] 5.3 补充小程序页面或组件测试，覆盖品牌卡片双列布局、Tab 切换、商品/证书空态和图片兜底。
- [x] 5.4 更新 `src/miniapp/README.md` 或相关长期文档，说明品牌入口页、品牌主页和接口调用边界。
- [x] 5.5 运行必要的 pytest、小程序静态测试和 OpenSpec 校验，并记录结果。
