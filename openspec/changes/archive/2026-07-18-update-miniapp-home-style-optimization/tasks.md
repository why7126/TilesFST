## 1. Change 准备与范围确认

- [x] 1.1 确认 `REQ-0043` trace 为 `in_sprint` 且 `iteration: sprint-008`，并确认本 Change 已纳入 `sprint-008`。
- [x] 1.2 对照 `REQ-0041` 当前小程序首页实现和 `BUG-0065` 运行入口修复状态，列出需要保留的既有接口、页面和测试。
- [x] 1.3 确认本 Change 不实现收藏持久化、证书聚合页、品牌馆独立页、新品榜/热销榜独立页和快捷入口后台配置。

## 2. 小程序首页视觉与结构

- [x] 2.1 将首页首屏调整为深色品牌视觉，覆盖页面背景、模块底色、品牌金、主文字、辅助文字和图片占位状态。
- [x] 2.2 实现品牌 Header、搜索入口和 Banner 区，并确保不模拟微信系统状态栏、分享按钮、关闭按钮或胶囊控件。
- [x] 2.3 将快捷入口固定为“选瓷砖、品牌馆、新品榜、热销榜”，实现统一图标在上、文案在下结构和安全降级跳转。
- [x] 2.4 调整新品推荐和热销推荐卡片，展示 SKU 主图、SKU 编号或名称、规格、空间/工艺标签和 `price_display`。

## 3. 全部产品瀑布流

- [x] 3.1 复用现有公开商品列表接口或首页数据源，实现全部产品首批加载。
- [x] 3.2 实现双列瀑布流布局，确保图片高度允许错落且不退化为固定等高网格。
- [x] 3.3 实现触底加载、并发请求防重、追加不覆盖、按 `product_id` 去重和页面离开后的过期回调保护。
- [x] 3.4 实现加载中、加载失败可重试、无更多、空商品和图片失败的模块级降级状态。

## 4. TabBar 与安全降级

- [x] 4.1 将小程序 TabBar 目标文案调整为：首页、分类、找砖、收藏、证书。
- [x] 4.2 为未完整实现的收藏和证书页面提供建设中提示或可返回占位页，避免白屏和路由错误。
- [x] 4.3 确认“我的”Tab 不作为首页目标导航出现；如仍需用户中心能力，记录为后续独立需求入口。

## 5. 埋点与 API 边界

- [x] 5.1 增加或预留首页搜索、快捷入口、新品/热销/全部产品点击、瀑布流加载/失败/到底、收藏视觉点击和证书 Tab 点击事件。
- [x] 5.2 确认事件字段不包含聊天内容、Authorization、Cookie、手机号、raw payload 或其它不必要敏感信息。
- [x] 5.3 若 `/api/v1/miniapp/products` 不能稳定支持分页状态，扩展响应字段并同步 OpenAPI、Orval、docs 和后端测试。

## 6. 验证与文档

- [x] 6.1 补充小程序静态或单元测试，覆盖首页运行入口非空、瀑布流状态机、TabBar 安全降级和关键跳转。
- [x] 6.2 补充后端 usage event 字典测试，覆盖新增事件、未知事件拒绝和禁止属性过滤。
- [x] 6.3 在 320 到 430 pt 宽度范围内完成首页视觉验收，确认无横向滚动、重叠、严重截断或 TabBar 遮挡。
- [x] 6.4 更新必要的 docs、trace 和验收记录；若 API 变更，同步 `docs/03-api-index.md`、`docs/04-database-design.md`、错误码文档和 Orval 生成物。
