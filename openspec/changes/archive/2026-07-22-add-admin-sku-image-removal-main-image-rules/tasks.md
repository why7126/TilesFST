## 1. 前端图片状态逻辑

- [x] 1.1 在 `TileSkuFormModal` 或相邻 helper 中实现图片数组 normalize：唯一主图、主图第一、`sort_order` 连续。
- [x] 1.2 调整“设为主图”逻辑，使目标图片即时成为主图并移动到第一位。
- [x] 1.3 为每张商品图片增加移除入口，支持移除主图和非主图。
- [x] 1.4 实现移除当前主图兜底：优先选择移除前后一张图片，否则选择剩余第一张。
- [x] 1.5 实现移除全部图片后的空列表状态，保留“继续添加图片”入口。
- [x] 1.6 确保 buildPayload 不包含已移除图片，且 images 中主图 `is_main=true`、`sort_order=0`。

## 2. UI 与横切验收

- [x] 2.1 保持 SKU 弹窗仅使用 `sku-modal-card` 等专属类，不引入通用 `modal-card` 双类挂载。
- [x] 2.2 验证 1440px 视口下 SKU 编辑弹窗 computed width 与既有 SKU 弹窗一致。
- [x] 2.3 验证矮视口下弹窗 body 可滚动，底部操作区和图片网格不遮挡。
- [x] 2.4 确认图片上传状态机、即时回显和上传失败控件内错误提示未因移除能力回归。
- [x] 2.5 若实现未触及上传链路，在验收记录中将 Docker 边界文件上传标记为 N/A 并说明原因；若触及上传链路则经 `http://localhost:3000` 验证。

## 3. 测试

- [x] 3.1 补充 `TileSkuFormModal` 组件测试：移除非主图后主图保持不变。
- [x] 3.2 补充组件测试：设为主图后目标图片前置且 payload 主图 `sort_order=0`。
- [x] 3.3 补充组件测试：移除当前主图后后一张图片自动成为新主图。
- [x] 3.4 补充组件测试：移除最后位置主图后剩余第一张成为新主图。
- [x] 3.5 补充组件测试：移除全部图片后 payload images 为空且添加入口仍可用。
- [x] 3.6 必要时补充后端回归测试，确认空 images、主图唯一和 sort_order 回填。

## 4. 文档与校验

- [x] 4.1 若 API contract 未变化，在执行输出中明确不需要 OpenAPI/Orval。
- [x] 4.2 若实现改变 images 请求/响应字段，同步 OpenAPI、Orval、API 文档和接口测试。
- [x] 4.3 运行相关前端测试；如后端逻辑调整，运行对应 pytest。
- [x] 4.4 更新 change `trace.md` 的实现证据、测试摘要和横切 AC 结论。
