## 1. API / 数据契约

- [x] 1.1 确认公开证书列表 API 契约，优先使用 `GET /api/v1/miniapp/certificates`，明确分页查询参数和响应字段。
- [x] 1.2 实现后端公开证书查询，过滤隐藏、软删除和所属品牌不可公开证书。
- [x] 1.3 确保证书文件 URL 为后端受控可读引用，不返回原始 object key、后台备注、审计字段或内部用户字段。
- [x] 1.4 去除公开证书列表搜索/筛选能力，并保证分页排序稳定。
- [x] 1.5 同步 OpenAPI、Orval、小程序服务层和 `docs/03-api-index.md` 相关接口索引。

## 2. 小程序页面实现

- [x] 2.1 将 `src/miniapp/pages/certificates/index.*` 从占位页改为公开证书列表页。
- [x] 2.2 接入全局 `custom-navigation`，标题为“证书”，并保证页面 offset、胶囊 reserve 和 TabBar 遮挡处理。
- [x] 2.3 实现首屏加载、下拉刷新、触底加载更多、无更多提示和重复请求防护。
- [x] 2.4 确保证书列表页不提供搜索功能和筛选功能。
- [x] 2.5 实现一行 2 个证书卡片，卡片信息仅展示证书名称、品牌名称、证书类型。
- [x] 2.6 实现暂无公开证书、网络失败、加载更多失败和图片失败降级状态。

## 3. 预览 / 埋点 / 安全

- [x] 3.1 实现图片证书预览，并在失败时展示稳定提示。
- [x] 3.2 确认并实现 PDF 证书受控打开、复制提示或等价预览方案。
- [x] 3.3 接入页面曝光、证书点击、预览点击和加载失败埋点。
- [x] 3.4 检查日志和埋点不记录 Authorization header、Cookie、密钥、`.env` 内容、本机路径或个人敏感信息。

## 4. 测试与验收

- [x] 4.1 补充后端接口测试，覆盖公开证书查询、隐藏过滤、软删除过滤、品牌不可公开过滤、内部字段排除和分页。
- [x] 4.2 补充小程序静态/单元测试，覆盖页面入口、两列列表渲染、无搜索筛选、空/错状态、重复请求防护和预览降级。
- [x] 4.3 补充或更新 `.js` / `.ts` 运行入口一致性校验，确保微信开发者工具实际加载脚本不是空模板。
- [x] 4.4 按 `docs/knowledge-base/best-practices/miniapp-custom-navigation.md` 记录 320/375/430 pt DevTools evidence；真机不可用时标记 blocked 或 follow_up。
- [x] 4.5 运行必要后端测试、小程序静态测试、OpenSpec 校验和 Workflow Sync。

## 5. 文档与追溯

- [x] 5.1 更新 REQ-0057 trace、Change trace、Sprint 验收材料和 AI usage hook 记录。
- [x] 5.2 若 API、DB、MinIO 或端范围发生超出 REQ-0057 的变化，回到需求评审或拆分 follow-up REQ/BUG。
