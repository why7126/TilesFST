## 1. OpenSpec 与追溯

- [x] 1.1 确认 `REQ-0046`、`sprint-008` 与 `add-miniapp-search-component` 双向追溯完整。
- [x] 1.2 复核 proposal、design、delta specs 与 tasks 不包含管理端搜索配置中心或 `/api/admin/search/*`。
- [x] 1.3 运行 OpenSpec 校验并修复规格格式问题。

## 2. API 与数据契约

- [x] 2.1 梳理现有公开 SKU、品牌、类目、规格、证书查询能力，确定搜索接口复用或新增策略。
- [x] 2.2 实现或确认小程序联想接口契约，覆盖关键词、scope、limit、requestId、实体类型和证书不参与联想。
- [x] 2.3 实现或确认小程序完整搜索接口契约，覆盖类型 Tab 数量、最佳匹配、分区结果、筛选 facets、分页和 hasMore。
- [x] 2.4 实现或确认热门词与最近浏览数据来源，明确 v1 不提供管理端维护入口。
- [x] 2.5 对所有搜索结果执行公开状态过滤，排除下架 SKU、停用品牌、停用类目、停用规格和不可公开证书。
- [x] 2.6 若新增或调整 API，同步 OpenAPI、Orval、docs 和接口测试。

## 3. 小程序搜索组件与页面

- [x] 3.1 新增或复用小程序搜索入口组件，支持关键词、清空、提交、取消/返回、禁用态、scope 和 sourcePage。
- [x] 3.2 接入首页、分类页、SKU 列表页、品牌相关页面和独立搜索页入口，分类页保留类目上下文。
- [x] 3.3 实现搜索首页，展示最近搜索、热门搜索和最近浏览。
- [x] 3.4 实现最近搜索本机保存、去重置顶、单条删除和全部清空。
- [x] 3.5 实现实时联想，覆盖输入阈值、300ms 防抖、旧请求丢弃和 6 到 10 条建议展示。
- [x] 3.6 实现联想点击跳转，直达实体进详情/列表，不可达目标安全降级到搜索结果页。
- [x] 3.7 实现搜索结果页，覆盖综合、SKU、品牌、类目、证书 Tab、最佳匹配和分区结果。
- [x] 3.8 实现 SKU 结果卡片，展示主图、产品名称、SKU 编码、品牌、规格、参考价格并整卡进入详情。
- [x] 3.9 实现快捷筛选和底部完整筛选抽屉，支持品牌、类目、规格、价格区间、重置、确认和结果数量反馈。
- [x] 3.10 实现无结果、加载、失败和重试状态，不展示联系商家、找砖、购物车、询价或在线下单入口。

## 4. 搜索埋点与安全

- [x] 4.1 扩展 usage event 字典，支持搜索页浏览、输入、联想曝光/点击、提交、结果曝光/点击、筛选、无结果和历史操作事件。
- [x] 4.2 接入小程序搜索埋点，事件携带 keyword、normalizedKeyword、scope、entityType、resultCount、sourcePage、filterSnapshot 和 requestId。
- [x] 4.3 后端校验阻断手机号、Authorization header、Cookie、raw payload、raw object key 等敏感字段。
- [x] 4.4 确保埋点失败不阻断搜索输入、联想、结果展示、筛选、无结果和历史操作主流程。

## 5. 原型视觉与小程序验收

- [x] 5.1 按 `prototype/*.html`、`prototype/*.png` 和 `prototype/context.md` 验收搜索首页、联想、结果、筛选和无结果。
- [x] 5.2 验证 390px 基准、320 到 430 pt 宽度、顶部安全区、44px 搜索框和主要触控区域。
- [x] 5.3 验证类型 Tab 横向滚动、筛选抽屉 72% 覆盖、62% 遮罩和品牌金选中态。
- [x] 5.4 验证搜索空状态、加载态、联想态、结果态、失败态切换无明显布局跳动。

## 6. 测试与文档

- [x] 6.1 补充后端接口测试，覆盖联想、完整搜索、公开状态过滤、筛选 facets、无结果和校验错误。
- [x] 6.2 补充 usage event 测试，覆盖搜索事件字典、敏感字段拒绝和失败不阻断。
- [x] 6.3 补充小程序静态或单元测试，覆盖搜索组件状态、最近搜索、联想防抖、旧请求丢弃、结果 Tab 和筛选抽屉。
- [x] 6.4 更新 API、数据、埋点或小程序文档，说明不包含管理端搜索配置中心和 `/api/admin/search/*`。
- [x] 6.5 汇总验收证据与命令输出摘要，保持成功路径 compact summary。
