---
purpose: 系统架构说明
content: 前后端模块、存储、部署、接口生成链路
source: project.yaml + AI自动生成
update_method: 架构变化时由AI辅助更新，架构负责人确认
note: 涉及服务拆分、依赖变化、接口生成方式变化时必须更新
---

# 系统架构

## 架构概览

系统由 Web展示端、Web管理端、微信小程序、FastAPI后端、SQLite数据库和MinIO对象存储组成。

## 前端

{% for item in frontend.stack %}
- {{ item }}
{% endfor %}

## 后端

{% for item in backend.stack %}
- {{ item }}
{% endfor %}

## 数据与文件

- 结构化数据存储：{{ storage.database }}
- 瓷砖图片、附件、素材存储：{{ storage.object_storage }}

## OpenAPI 与 Orval

后端通过 FastAPI 自动生成 OpenAPI，前端通过 Orval 生成类型定义与 API 客户端。
