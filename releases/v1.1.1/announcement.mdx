---
title: "产品版本 v1.1.1"
created_at: 2026-08-12 22:12:00
updated_at: 2026-08-12 22:54:00
status: generated
---

# 产品版本 v1.1.1

发布时间：2026-08-12 22:12:00

关联 Sprint：sprint-023

## 新增功能

- 管理端性能观测新增筛选候选值能力，支持按时间范围获取端类型、版本号、页面、网络和指标等候选项。
- 管理端性能观测筛选区调整为“时间范围 > 端类型 > 版本号 > 页面 > 网络 > 指标”，并同步优化聚合列表和样本页字段顺序。
- 沉淀小程序媒体四联验收最佳实践，补齐证据链、测试 helper 与审计检查，提升后续媒体验收的一致性。
- 建立管理端列表列展示、默认 nowrap、冻结操作列和分页 DOM 一致性契约，减少列表页横切回归。

## 修复 BUG

- 修复小程序 RUM 上报中版本号、request_id、指标标签、空态展示和聚合分组口径不一致的问题。
- 修复小程序开发环境 RUM 上报 API 地址选择问题，使本地调试时复用项目端口配置和可用地址缓存策略。

## 发布注意事项

- 本版本是小补丁，已确认不生成或更新产品使用文档。
- 镜像构建计划与镜像构建证据已完成；最终镜像校验以 `releases/v1.1.1/image-manifest.json` 和离线包 `.sha256` sidecar 为准。
- 用户可见产品版本已同步为 `v1.1.1`。
- 本版本不声明数据库表结构、索引或迁移变更。

## 已知问题

- 暂无已知问题。

## 升级步骤

1. 完成升级前备份。
2. 确认镜像构建计划、镜像 manifest 与离线包校验文件均为本版本产物。
3. 校验离线包 `.sha256` sidecar。
4. 部署新版本。
5. 执行小程序 RUM、管理端性能观测、管理端列表页分页和筛选路径的 smoke 与回归验证。

## 回滚说明

1. 回退到上一 Git tag 或上一部署包。
2. 恢复上一版本环境变量、Compose 配置和镜像 tag。
3. 若发现数据影响，按项目数据库备份与回滚流程处理。

## 影响范围

| 范围 | 影响 |
|---|---|
| Web 管理端 | 性能观测筛选候选值、字段顺序、列表列展示与分页一致性契约 |
| 店主 Web | 不涉及店主 Web 展示端用户功能 |
| 小程序 | RUM 版本号、request_id、指标标签和开发环境 API 地址策略 |
| 后端 | 管理端性能事件候选值接口与发布治理脚本 |
| 数据库 | 不涉及数据库结构变更 |
| 对象存储 | 沉淀媒体验收与对象审计最佳实践，不新增运行时能力 |
| Docker | 镜像构建计划、镜像 manifest 与离线包校验文件已生成 |
